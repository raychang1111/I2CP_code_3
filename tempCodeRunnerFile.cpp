 // if(0<=a-2 && 0<=b-1)
    //     enable[a-2][b-1]=true;
    // if(0<=a-2 && b+1<=m)
    //     enable[a-2][b+1]=true;
    // if(a+2<=n && 0<=b-1)
    //     enable[a+2][b-1]=true;
    // if(a+2<=n && b+1<=m)
    //     enable[a+2][b+1]=true;
    // if(0<=b-2 && 0<=a-1)
    //     enable[a-1][b-2]=true;
    // if(0<=b-2 && a+1<=n)
    //     enable[a+1][b-2]=true;
    // if(b+2<=m && 0<=a-1)
    //     enable[a-1][b+2]=true;
    // if(b+2<=m && a+1<=n)
    //     enable[a+1][b+2]=true;